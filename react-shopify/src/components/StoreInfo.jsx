const StoreInfo = () => {
    const name = "Bao Store";
    const owner = "Bao";
    const product = 123;

    return (
        <div>
            <p>{name}</p>
            <p>{owner}</p>
            <p>{product}</p>
        </div>
    )
}

export default StoreInfo